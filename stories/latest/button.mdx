import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";
import { Button } from "@digital-science/figshare-fcl/latest/Button";
import * as ButtonStories from "./button.stories.jsx";
import { Cover } from "../story-utils/Cover";

<Meta of={ButtonStories} />

# Button

---

- [Overview](#overview)
- [Props](#props-table)
- [Sub-Components](#sub-components)
- [Examples](#examples)

## Overview

A versatile button component with multiple kinds (primary, secondary, tertiary, inverted, input), emphasis levels (high, low, success, danger), and layout options.

<Canvas of={ButtonStories.Overview} />

```jsx
import { Button } from "@digital-science/figshare-fcl/latest/Button";

export default function ButtonOverview() {
  return (
    <div style={{ display: "flex", gap: "12px" }}>
      <Button kind="primary">
        <Button.Label>Primary</Button.Label>
      </Button>
      <Button kind="secondary">
        <Button.Label>Secondary</Button.Label>
      </Button>
      <Button kind="tertiary">
        <Button.Label>Tertiary</Button.Label>
      </Button>
    </div>
  );
}
```

The component can be imported from `@digital-science/figshare-fcl/latest`.

## Props Table

<ArgTypes of={Button} />

## Sub-Components

The Button component exports several sub-components:

- **Button.Label** - Text content wrapper with wrapping and ellipsis options
- **Button.Icon** - Icon slot with sizing and blending options
- **Button.InteractiveIcon** - Interactive icon with click and keyboard handlers
- **Button.IconGroup** - Groups multiple icons with proper spacing
- **Button.Loading** - Shows loading state with spinner and optional text

## Examples

### Primary Buttons

<Canvas of={ButtonStories.PrimaryButtons} />

```jsx
<Button kind="primary" em="high">
  <Button.Label>Primary</Button.Label>
</Button>

<Button kind="primary" em="high" disabled>
  <Button.Label>Disabled</Button.Label>
</Button>

<Button kind="primary" em="high" loading>
  <Button.Label>Loading</Button.Label>
</Button>

<Button kind="primary" em="success">
  <Button.Label>Success</Button.Label>
</Button>

<Button kind="primary" em="danger">
  <Button.Label>Danger</Button.Label>
</Button>
```

### Secondary Buttons

<Canvas of={ButtonStories.SecondaryButtons} />

```jsx
<Button kind="secondary" em="high">
  <Button.Label>Secondary</Button.Label>
</Button>

<Button kind="secondary" em="low">
  <Button.Label>Secondary Low</Button.Label>
</Button>

<Button kind="secondary" em="success">
  <Button.Label>Success</Button.Label>
</Button>

<Button kind="secondary" em="danger">
  <Button.Label>Danger</Button.Label>
</Button>
```

### Tertiary Buttons

<Canvas of={ButtonStories.TertiaryButtons} />

```jsx
<Button kind="tertiary" em="high">
  <Button.Label>Tertiary</Button.Label>
</Button>

<Button kind="tertiary" em="low">
  <Button.Label>Tertiary Low</Button.Label>
</Button>

<Button kind="tertiary" em="danger">
  <Button.Label>Danger</Button.Label>
</Button>
```

### Inverted Buttons

<Cover kind={"card"}>
  {() => (
    <div style={{ display: "flex", flexDirection: "column", gap: "24px", backgroundColor: "#333", padding: "20px", borderRadius: "4px" }}>
      <div>
        <h4 style={{ color: "#fff" }}>Inverted - High Emphasis</h4>
        <div style={{ display: "flex", flexDirection: "row", gap: "12px" }}>
          <Button kind="inverted" em="high"><Button.Label>Inverted</Button.Label></Button>
          <Button kind="inverted" em="high" disabled><Button.Label>Disabled</Button.Label></Button>
        </div>
      </div>
      <div>
        <h4 style={{ color: "#fff" }}>Inverted - Low Emphasis</h4>
        <div style={{ display: "flex", flexDirection: "row", gap: "12px" }}>
          <Button kind="inverted" em="low"><Button.Label>Inverted Low</Button.Label></Button>
          <Button kind="inverted" em="low" disabled><Button.Label>Disabled</Button.Label></Button>
        </div>
      </div>
    </div>
  )}
</Cover>

```jsx
<Button kind="inverted" em="high">
  <Button.Label>Inverted</Button.Label>
</Button>

<Button kind="inverted" em="high" disabled>
  <Button.Label>Disabled</Button.Label>
</Button>

<Button kind="inverted" em="low">
  <Button.Label>Inverted Low</Button.Label>
</Button>
```

### Button with Icons

<Canvas of={ButtonStories.ButtonWithIcons} />

```jsx
import { Button } from "@digital-science/figshare-fcl/latest/Button";
import { Eye, Edit, Delete, Checkmark, Download, Copy } from "@digital-science/figshare-fcl/icons/react";

// Icon only buttons
<Button kind="primary" em="high" span="icon" title="View">
  <Button.Icon><Eye /></Button.Icon>
</Button>

// Icon with label
<Button kind="primary" em="high">
  <Button.Icon><Download /></Button.Icon>
  <Button.Label>Download</Button.Label>
</Button>

<Button kind="secondary" em="high">
  <Button.Icon><Copy /></Button.Icon>
  <Button.Label>Copy</Button.Label>
</Button>

<Button kind="tertiary" em="high">
  <Button.Icon><Eye /></Button.Icon>
  <Button.Label>Preview</Button.Label>
</Button>
```

### Button Sizes and Spans

<Canvas of={ButtonStories.ButtonSizesAndSpans} />

```jsx
import { Button } from "@digital-science/figshare-fcl/latest/Button";

// Full width
<Button kind="primary" span="full">
  <Button.Label>Full Width Button</Button.Label>
</Button>

// Half width
<Button kind="secondary" span="half">
  <Button.Label>Half Width</Button.Label>
</Button>

// Icon only
<Button kind="primary" span="icon" title="Confirm">
  <Button.Icon><Checkmark /></Button.Icon>
</Button>
```

### Inline Buttons

<Canvas of={ButtonStories.InlineButtons} />

```jsx
import { Button } from "@digital-science/figshare-fcl/latest/Button";

// Regular inline button
<p>This is a paragraph with an <Button kind="tertiary" inline={true}><Button.Label>inline button</Button.Label></Button> in the text.</p>

// Slim inline button
<p>This is a paragraph with a <Button kind="primary" inline="slim"><Button.Label>slim primary button</Button.Label></Button> in the text.</p>

<p>This is a paragraph with a <Button kind="secondary" inline="slim"><Button.Label>slim secondary button</Button.Label></Button> in the text.</p>
```

### Loading States

<Canvas of={ButtonStories.LoadingStates} />

```jsx
import { Button } from "@digital-science/figshare-fcl/latest/Button";

<Button kind="primary" loading={true}>
  <Button.Label>Processing</Button.Label>
</Button>

<Button kind="secondary" loading={true}>
  <Button.Label>Loading</Button.Label>
</Button>

<Button kind="tertiary" loading={true}>
  <Button.Label>Wait</Button.Label>
</Button>
```

### Tooltips

<Canvas of={ButtonStories.Tooltips} />

```jsx
import { Button } from "@digital-science/figshare-fcl/latest/Button";
import { Download, Edit, Delete } from "@digital-science/figshare-fcl/icons/react";

// Tooltip appears on hover via the title attribute
<Button kind="primary" span="icon" title="Download the file">
  <Button.Icon><Download /></Button.Icon>
</Button>

<Button kind="secondary" span="icon" title="Edit this item">
  <Button.Icon><Edit /></Button.Icon>
</Button>

<Button kind="tertiary" span="icon" title="Delete permanently">
  <Button.Icon><Delete /></Button.Icon>
</Button>
```
