import { disclosure } from "./disclosure.stories";
import { note } from "./note.stories";
import { rule } from "./rule.stories";

export const Disclosure = (args) => <Fragment>{disclosure(args)}</Fragment>
export const Note = (args) => <Fragment>{note(args)}</Fragment>
export const Rule = (args) => <Fragment>{rule(args)}</Fragment>
