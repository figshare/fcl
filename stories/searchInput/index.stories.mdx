
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import SearchInput from "@figshare/ui/searchInput";
import uncontrollable from "@figshare/ui/helpers/uncontrollable";

<Meta 
  title="UI/Inputs/Search" 
  component={SearchInput}
  parameters={{
    viewMode: 'docs',
    previewTabs: { 
      canvas: { hidden: true } 
     },
  }} 
/>

# Search input
---


export const UncontrolledInput = uncontrollable(
  SearchInput,
  { value: ["onChange", (_, e) => e.target.value] }
);

export const mountSearchInput = (props) => <UncontrolledInput{...props} />;

<Story 
  name="Search" 
  parameters={{
    layout: "centered",
    jest: ["ui/src/searchInput"],
  }}
>
  {mountSearchInput()}
</Story>


```jsx
import React from "react";
import SearchInput from "@figshare/ui/searchInput";

class SearchForm extends React.Component {
  render() {
    return (
      <SearchInput />
    );
  }
}
```

<ArgsTable of={SearchInput} />


## Themes

<Canvas>
  <UncontrolledInput size="S" theme="default" placeholder="Default theme" />
  <UncontrolledInput size="S" theme="underline" placeholder="Underline theme" />
  <UncontrolledInput size="S" theme="default" error={true} placeholder="Default theme - error" />
  <UncontrolledInput size="S" theme="underline" error={true} placeholder="Underline theme - error" />
  <UncontrolledInput size="S" theme="default" disabled={true} placeholder="Default theme - disabled" />
  <UncontrolledInput size="S" theme="underline" disabled={true} placeholder="Underline theme - disabled" />
</Canvas>

<Canvas>
  <UncontrolledInput size="L" theme="default" placeholder="Default theme" />
  <UncontrolledInput size="L" theme="underline" placeholder="Underline theme" />
  <UncontrolledInput size="L" theme="default" error={true} placeholder="Default theme - error" />
  <UncontrolledInput size="L" theme="underline" error={true} placeholder="Underline theme - error" />
  <UncontrolledInput size="L" theme="default" disabled={true} placeholder="Default theme - disabled" />
  <UncontrolledInput size="L" theme="underline" disabled={true} placeholder="Underline theme - disabled" />
</Canvas>