
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import uncontrollable from "@figshare/ui/helpers/uncontrollable";

<Meta 
  title="UI/Textarea" 
  component={Textarea}
  parameters={{
    viewMode: 'docs',
    previewTabs: { 
      canvas: { hidden: true } 
     },
  }}
/>

# Textarea
---

import Textarea from "@figshare/ui/textarea";

export const UncontrolledTextarea = uncontrollable(
  Textarea,
  { value: ["onChange", (_, e) => e.target.value] }
);

export const mountTextarea = (props) => <UncontrolledTextarea{...props} />;

<Story 
  name="Textarea" 
  parameters={{
    layout: "centered",
    jest: ["ui/src/textarea"],
  }}
>
  {mountTextarea()}
</Story>


```jsx
import React from "react";
import Textarea from "@figshare/ui/textarea";

class TextareaComponent extends React.Component {
  render() {
    return (
      <Textarea />
    );
  }
}
```

<ArgsTable of={Textarea} />


## Themes

<Canvas>
  <UncontrolledTextarea theme="default" placeholder="Default theme" />
  <UncontrolledTextarea theme="underline" placeholder="Underline theme" />
  <UncontrolledTextarea theme="default" error={true} placeholder="Default theme - error" />
  <UncontrolledTextarea theme="underline" error={true} placeholder="Underline theme - error" />
  <UncontrolledTextarea theme="default" disabled={true} placeholder="Default theme - disabled" />
  <UncontrolledTextarea theme="underline" disabled={true} placeholder="Underline theme - disabled" />
</Canvas>
